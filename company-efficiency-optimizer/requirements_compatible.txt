# Compatible versions for Company Efficiency Optimizer
# Pinned to Pydantic v1 compatible versions

# Core CrewAI and AI dependencies (Pydantic v1 compatible)
crewai==0.1.32
langchain==0.1.0
langchain-core==0.1.23
langchain-community==0.0.20
langchain-openai==0.0.2.post1

# Pydantic v1 (required for CrewAI 0.1.32)
pydantic==1.10.13
pydantic-core==2.14.6

# Data processing and analysis
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0

# Vector database and embeddings
pinecone-client>=3.0.0
PyYAML>=6.0.0

# PDF processing and OCR (compatible versions)
pytesseract>=0.3.10
pdf2image>=1.16.0,<3.0.0
Pillow>=10.0.0

# HTTP requests and API integration
requests>=2.31.0
Flask>=3.0.0

# Additional utilities
rich>=13.0.0
typer>=0.9.0

# Optional: NVIDIA integration (compatible versions)
# langchain-nvidia-ai-endpoints==0.1.0  # Uncomment if needed